# Frontend staging env — these are baked into the build at image-build time.
# In Dokploy, set them as Build Args (not runtime env) on the Application service.

VITE_API_URL=https://staging-api.example.com/api
VITE_PAYSTACK_PUBLIC_KEY=pk_test_replace_me
